# Local connection for this workstation. Never commit a real credential.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-6-astra
# Optional shared clinic service (HTTPS unless it runs on loopback).
# RADAGENT_BACKEND_URL=https://reporting.example.org
# RADAGENT_BACKEND_TOKEN=
